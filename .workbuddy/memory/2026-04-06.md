# 2026-04-06 工作日志

## 编译修复记录 (FlinkRulInference.scala)
- **FlinkKafkaProducer 构造函数错误**: SimpleStringSchema 不被接受, 改用 KafkaSerializationSchema 匿名类包装, 语义从 EXACTLY_ONCE 改为 AT_LEAST_ONCE
- **JSON.toJSONString 歧义**: Scala 无法区分 fastjson 两个重载, 改用 JSON.toJSON(r).toString 绕过
- **HighFreqAccumulator case class → 普通 class**: case class 在 AggregateFunction.add 中类型不匹配(Flink 1.14要求返回accumulator而非Unit), 改为普通class+def this()无参构造
- **add 方法返回值**: 从 Unit 改为 HighFreqAccumulator(返回acc)
- **RealtimeEngine.scala HDFS 端口**: 从 master:9000 改为 master:8020(与Flume一致)
- 最终 mvn clean package BUILD SUCCESS, 输出 shaded jar (realtime-data-process-1.0-SNAPSHOT.jar)

## 双引擎独立架构确认
- **方案确定**: Spark + Flink 双引擎独立运行, 4个数据生成脚本全部启动
- **Spark 引擎** (RealtimeEngine.scala): 消费 sensor_raw + log_raw + ChangeRecord
  - 数据源: generate_data.sh (0.3秒/条, 11台设备101-105/112/114-118)
  - Flume 配置: Kafka + HDFS 双写备份
- **Flink 引擎** (FlinkRulInference.scala v2.0): 消费 device_state + sensor_metrics + highfreq_sensor
  - 数据源: generate_change_data.sh (3秒/条) + generate_highfreq_sensor.sh (1秒/条), 9台设备109-117
  - 输出: warning_log topic (仅报警数据)
  - 高频窗口聚合7特征

## MyFlume.conf 更新为全量配置
- 合并到1个agent: 6个source / 9个channel / 9个sink
- 旧3源: Kafka+HDFS双写; 新3源: 仅Kafka无HDFS
- Flume 已在服务器启动成功, 9个sink全部正常

## Flink 引擎本地运行成功 (18:42)
- **问题**: IDEA 直接运行报 CreateProcess error=206 (Windows命令行长度超限)
- **解决**: 运行配置 → 修改选项 → "缩短命令行" 选 "JAR 清单"
- **运行结果**: 成功启动, 订阅3个topic(device_state/sensor_metrics/highfreq_sensor), 输出warning_log
- **实时报警验证**: 收到设备109/111/112/113/114/116的报警数据, AlarmRecord 22字段完整
- **高频特征**: 前7个高频字段暂时为0.0(需等5分钟窗口滚动后才有聚合值)
- **双引擎状态**: ✅Flink已跑通, ⏳Spark RealtimeEngine待启动
